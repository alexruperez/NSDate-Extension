language: objective-c
before_install:
    - brew update
    - if brew outdated | grep -qx xctool; then brew upgrade xctool; fi
script: xctool -project NSDate-Extension\ Example.xcodeproj -scheme NSDate-Extension\ Example -sdk iphonesimulator clean build test